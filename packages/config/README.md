# @ai-job-hunter/config

Centralized path and environment configuration shared across apps and pipelines.

## Usage

### TypeScript (dashboard)

```ts
import { PATHS } from '@ai-job-hunter/config';
```

### Python

```python
from packages.config.python.paths import DATA_FILE, DASHBOARD_DIST, REPO_ROOT
```
