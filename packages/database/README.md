# @ai-job-hunter/database

Typed Supabase access for dashboard (TypeScript) and pipeline (Python).

## TypeScript (GitHub Pages)

```ts
import { createBrowserClient, DashboardRepository } from '@ai-job-hunter/database';
```

## Python (GitHub Actions scraper)

```python
from packages.database.python import JobRepository, create_service_client, is_supabase_configured
```
