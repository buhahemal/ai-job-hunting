name: CI

# Umbrella workflow — triggers all quality gates in parallel on PR/push.
# Individual workflows also exist for granular visibility in Actions tab.

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  HUSKY: 0
  VITE_BASE_PATH: /${{ github.event.repository.name }}/

jobs:
  lint:
    uses: ./.github/workflows/internal/lint.yml
  tests:
    uses: ./.github/workflows/internal/tests.yml
  build:
    uses: ./.github/workflows/internal/build.yml
  docker:
    uses: ./.github/workflows/internal/docker.yml
  docs:
    uses: ./.github/workflows/internal/docs.yml
  markdown:
    uses: ./.github/workflows/internal/markdown.yml
  format:
    uses: ./.github/workflows/internal/format.yml
  gitleaks:
    uses: ./.github/workflows/internal/gitleaks.yml
  duplicate-code:
    uses: ./.github/workflows/internal/duplicate-code.yml
  dead-code:
    uses: ./.github/workflows/internal/dead-code.yml
  architecture:
    uses: ./.github/workflows/internal/architecture.yml
  semgrep:
    uses: ./.github/workflows/internal/semgrep.yml
  spellcheck:
    uses: ./.github/workflows/internal/spellcheck.yml
  actionlint:
    uses: ./.github/workflows/internal/actionlint.yml
